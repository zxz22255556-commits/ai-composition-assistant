# 腾讯云 CloudBase 部署指南

> 为什么用腾讯云？Netlify 服务器在海外，访问国内火山引擎 API 会超时。腾讯云服务器在国内，访问火山引擎完全没问题。

## 一、准备工作

1. 注册/登录 [腾讯云](https://cloud.tencent.com/)
2. 完成实名认证（必须，否则无法使用云函数）
3. 打开 [云开发 CloudBase 控制台](https://console.cloud.tencent.com/tcb)

## 二、创建环境

1. 点「新建环境」
2. 环境名称随便填，比如 `ai-composition`
3. 版本选择：
   - **免费体验版**（0元/月，3000资源点）—— 轻度使用够
   - **个人版**（19.9元/月，40000资源点）—— 推荐，新用户常有0元体验活动
4. 点「立即开通」，等待环境创建完成（1-2分钟）

## 三、创建云函数（AI 分析后端）

### 3.1 上传代码

1. 进入环境 → 左侧「云函数」→「函数服务」
2. 点「新建云函数」
3. 填写：
   - 函数名称：`analyze`
   - 运行环境：`Nodejs 16.13`（或更高版本）
   - 创建方式：「本地上传文件夹」
   - 函数代码：选择本项目的 `cloudbase/functions/analyze` 文件夹
4. 点「下一步」→「完成」

### 3.2 配置环境变量

1. 函数列表里点 `analyze` 进入详情
2. 点「函数配置」→「编辑」
3. 找到「环境变量」，添加两个：
   - `AI_API_KEY` = 你的豆包 API Key（`ark-xxxxxx`）
   - `AI_MODEL` = 你的视觉模型接入点 ID（`ep-xxxxxx`）
4. 找到「超时时间」，改成 **30 秒**（AI 调用需要时间）
5. 点「保存」

### 3.3 配置 HTTP 访问路径

1. 函数详情页 → 点「触发管理」
2. 点「创建触发器」
3. 触发方式选「HTTP 访问服务」
4. 路径填 `/analyze`
5. 点「提交」
6. 复制生成的访问路径（类似 `https://xxx.service.tcloudbase.com/analyze`）

## 四、部署静态网站（前端）

1. 左侧「静态网站托管」→ 点「开通」（如果没开通）
2. 点「上传文件」，上传本项目 `static/index.html`
3. 上传后点「设置」→「基础配置」
4. 「索引文档」填 `index.html`，「错误文档」也填 `index.html`
5. 保存

## 五、修改前端 API 地址

> 如果你用的是同一个 CloudBase 环境的静态托管和云函数，前端默认路径 `/analyze` 应该能直接用。如果不行，需要手动改。

1. 打开 `static/index.html`
2. 找到 `const API_PATH = '/analyze';`
3. 如果相对路径不行，改成云函数的完整 HTTP 路径（3.3 步复制的那个）
4. 重新上传到静态网站托管

## 六、手机访问

1. 静态网站托管页面能看到一个默认域名（类似 `https://xxx-xxx.tcloudbaseapp.com`）
2. 手机 Safari 打开这个地址
3. 点「开启相机」→ 允许摄像头权限
4. 点击画面选主体，AI 会给出目标位置
5. 移动相机对齐目标点，绿点变金色就是对齐了
6. 点底部快门按钮拍照
7. 分享 →「添加到主屏幕」，桌面就有 App 图标了

## 常见问题

**Q：云函数调用报错？**
A：函数详情页 →「日志」→「运行日志」，看具体报错。最常见是环境变量没配对，或者 API Key 错了。

**Q：静态网站和云函数不同域名导致跨域？**
A：云函数代码里已经加了 `Access-Control-Allow-Origin: *`，允许跨域。如果还是不行，把前端的 API_PATH 改成云函数的完整 URL。

**Q：免费额度够吗？**
A：免费体验版 3000 资源点/月（约3元额度），每次云函数调用约消耗几资源点，每天用几十次完全够。不够可以升个人版 19.9元/月。

**Q：拍照后图片存在哪？**
A：网页拍照后会自动下载到手机，在「文件」App 或相册里能找到。
